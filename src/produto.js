export default class Produto{
    // Passando os parametros do dialogflow
    constructor(generoRoupa, nomeRoupa, infoRoupa, quantidadeItens, valorRoupa, adicionaProdutoDeDezEmDez){
        this.genero = generoRoupa;
        this.nome = nomeRoupa;
        this.informacao = infoRoupa;
        this.quantidade = quantidadeItens;
        this.valor = valorRoupa;
    }

    


}